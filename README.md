# Patrones

Паттерн-тренажёр испанской грамматики. Vue 3 + Vite, карточки из JSON-файлов в `decks/`, прогресс, настройки и банк ошибок — в **IndexedDB** в браузере.

Спека формата колод: [`RULES.md`](./RULES.md).

---

## Быстрый старт

```bash
npm install
npm run dev        # http://localhost:5173
npm run build      # production-сборка в dist/
npm run preview    # превью сборки
npm test           # unit-тесты (vitest)
npm run test:coverage
```

---

## Экраны

| Экран | Описание |
|-------|----------|
| **Настройка** | Выбор юнитов, блоков, режима прогона и параметров сессии |
| **Дрилл** | Карточка за карточкой: вопрос → ответ → оценка |
| **¡Listo!** | Итог сессии: «Прогнать ещё раз» или «К выбору колод» |

Переключатель **☀ / ☾ Тема** в шапке — светлая / тёмная тема (по умолчанию тёмная). Настройки UI и выбор юнитов/блоков **сохраняются** в IndexedDB между перезагрузками.

---

## Настройка прогона

Панель разбита на две вкладки: **Контент** и **Режим**.

### Контент

- **Тип карточек** — фильтр по `mode` блока: `transform`, `vocab`, `cloze`, `phrase`. Можно включить несколько или все.
- **Юниты** — список файлов из `decks/*.json`. Кнопка-переключатель в строке включает/выключает юнит.
- Полоска **изученности** и процент — по прогрессу карточек с `uuid` в выбранном направлении.
- Кнопки **все** / **снять** — выделить или сбросить все юниты.
- **сводка** — попап с HTML-сводкой юнита (поле `summary` в JSON).
- **блоки** — попап выбора отдельных блоков внутри юнита (вкл/выкл блоки, «все» / «снять» внутри попапа). Закрытие: кнопка «Готово», крестик, клик по оверлею, `Esc`. При снятии юнита попап закрывается автоматически.
- Счётчик в кнопке старта показывает число пар с учётом выбранных юнитов, блоков и фильтра типов.

### Режим

- **Порядок карточек** — 6 режимов (см. ниже).
- **Сессия** — направление, озвучка, повтор ошибок, таймер.
- **Данные** — кнопка **Экспорт JSON**: скачивает файл с прогрессом, банком ошибок и настройками.

Внизу экрана — две кнопки старта:

- **Поработать с ошибками** — отдельная сессия по банку ошибок (до 10 карточек).
- **Начать прогон** — обычная сессия по выбранному режиму порядка.

---

## Режимы порядка (6)

| # | Режим | Поведение |
|---|-------|-----------|
| 1 | **Всё по порядку** | Юниты по очереди → блоки прямо → карточки прямо |
| 2 | **Карточки вперемешку** | Юниты по очереди → блоки прямо → карточки внутри блока случайно |
| 3 | **Блоки и карточки вперемешку** | Юниты по очереди → блоки случайно → карточки внутри блока случайно |
| 4 | **Полный хаос** | Все карточки выбранных юнитов в один случайный поток |
| 5 | **Ревью** | По **одной** случайной карточке из каждого активного блока выбранных юнитов |
| 6 | **Слабые** | До **10** карточек с высокой долей ошибок по прогрессу (IndexedDB). Счётчик `(N)` в подписи режима. Учитываются выбранные юниты, фильтр типов и текущее направление |

Режим **6** отключает кнопку старта, если слабых карточек нет.

### Банк ошибок (отдельная кнопка)

- До **10** карточек из банка ошибок (IndexedDB). Счётчик `(N)` в подписи кнопки.
- Ошибся — снова в очередь; «Знал» — убирается из банка.
- Направление берётся из сохранённой ошибки.
- Кнопка отключается, если банк пуст.

---

## Параметры сессии

| Параметр | Описание |
|----------|----------|
| **Направление** | `front → back` или `back → front` |
| **Озвучивать ответ** | При показе ответа — `speechSynthesis` (es-ES, предпочтение голоса `es-ES`) |
| **Повторять ошибки** | При «Споткнулся» карточка возвращается в хвост очереди (в обычных режимах; в режиме ошибок — всегда) |
| **Таймер** | `вручную` или **1–5 сек** на вопрос + фиксированная **1 сек** на ответ после раскрытия |

### Таймерный режим

- Вопрос открывается автоматически по истечении таймера.
- После раскрытия — 1 с на просмотр ответа, затем переход к следующей карточке **без оценки** (прогресс и банк ошибок при авто-переходе **не** обновляются).
- **Клик по карточке** — пауза / продолжение таймера.
- Полоска-таймер на карточке.
- Кнопки «Споткнулся» / «Знал» скрыты — только авто-переход.

---

## Дрилл (ручной режим)

- **Показать ответ** — раскрывает `back` (или обратную сторону при `rev`).
- **Споткнулся** / **Знал** — оценка после раскрытия.
- На стороне вопроса может показываться `translation` (по правилам типа карточки).
- После раскрытия — `note`, кнопка **🔊 произнести**.
- Полоса прогресса, счётчики «осталось» и «споткнулся».
- Подпись юнита в `secbar`.

### Горячие клавиши (только в дрилле, без таймера)

| Клавиша | Действие |
|---------|----------|
| `Пробел` / `Enter` | Показать ответ |
| `←` / `1` | Споткнулся |
| `→` / `2` | Знал |
| `S` | Озвучить испанский текст |
| `Esc` | Выход к настройке |

---

## Типы карточек (`mode`)

| mode | Стороны | Смысл |
|------|---------|-------|
| **transform** | ES → ES | Преобразование формы (`el niño → la niña`) |
| **vocab** | RU → ES | Слово по переводу (`город → una ciudad`) |
| **cloze** | фраза с `___` → заполнение | `back` — только подстановка; пусто = `∅` |
| **phrase** | ES → ES | Цельная реплика (`¿Qué es…? → Es una…`) |

Логика отображения сторон (`sideFor`) учитывает направление и тип: для `vocab` вопрос — русский `front`, ответ — испанский `back`.

---

## Формат колод

- Файлы: `decks/unidad-NN.json`, подхватываются через Vite `import.meta.glob` при сборке.
- Структура: `unit` + массив `blocks[]` с `title`, `mode`, `cards[]`.
- Опционально: поле `summary` (HTML) — сводка по юниту в попапе.
- У каждой карточки обязателен уникальный **`uuid`** (v4) — без него прогресс, изученность и банк ошибок для карточки не ведутся.
- Первый юнит в списке включается по умолчанию при первой загрузке (если нет сохранённых настроек).
- Битые JSON попадают в сообщение об ошибке на экране настройки.

Подробные правила нарезки блоков и оформления JSON — в [`RULES.md`](./RULES.md).

---

## Хранение данных (IndexedDB)

База **`patrones`**, версия **3**. Данные локальные, привязаны к origin браузера. Очистка данных сайта в браузере удалит прогресс, ошибки и настройки.

### Store `lessons` — прогресс

- Ключ: `lessonId` (имя файла, напр. `unidad-01.json`).
- На карточку (`uuid`): отдельная статистика для `fwd` и `rev` — `correct`, `wrong`, `streak`, `lastSeenAt`.
- Обновляется при каждой ручной оценке «Знал» / «Споткнулся».

**Режим «Слабые»** — карточки, у которых:

- не меньше **2** попыток в выбранном направлении;
- weakness = `wrong / (attempts + 2)` ≥ **0.3**.

Сортировка по убыванию weakness, в сессию — до **10** штук.

**Изученность юнита** — агрегат по карточкам с `uuid`: учитываются попытки, серия, давность последнего просмотра.

### Store `mistakes` — банк ошибок

- Ключ: `uuid` + направление (`fwd` / `rev`).
- Запись при «Споткнулся» (с `missCount`, `lastMissedAt`, снимок карточки).
- Удаление при «Знал» в сессии **«Поработать с ошибками»**.
- В сессию — до **10** последних по `lastMissedAt`.

### Store `settings` — настройки UI

- Ключ: `ui`.
- Тема, вкладка настройки, режим порядка, направление, таймер, фильтр типов, выбор юнитов и блоков.
- Автосохранение с debounce ~250 мс при изменениях.

### Экспорт

Кнопка **Экспорт JSON** (вкладка «Режим») скачивает файл `patrones-backup-YYYY-MM-DD.json`:

```json
{
  "format": "patrones-backup",
  "version": 1,
  "exportedAt": 0,
  "dbVersion": 3,
  "lessons": [],
  "mistakes": [],
  "settings": {}
}
```

Импорт из файла **не реализован**.

Миграции со старых форматов (localStorage и т.п.) **нет**. Старый режим порядка `mistakes` при загрузке настроек маппится в `straight`.

---

## Что не реализовано

- Импорт резервной копии JSON.
- Кнопка «Повторить ошибки» на экране ¡Listo! (только общий рестарт и возврат к настройке).
- Запись прогресса / ошибок при авто-переходе по таймеру.

---

## Структура проекта

```
patrones/
├── decks/              # JSON-колоды (unidad-01 … unidad-07)
├── src/
│   ├── App.vue         # Состояние приложения, дрилл-логика
│   ├── components/
│   │   ├── SetupView.vue
│   │   ├── DrillView.vue
│   │   └── DoneView.vue
│   ├── lib/
│   │   ├── loadDecks.ts    # Загрузка decks/*.json
│   │   ├── patrones.ts     # Очередь, парсинг, sideFor
│   │   ├── queue.ts
│   │   ├── idb.ts          # IndexedDB
│   │   ├── progress.ts     # Прогресс и «слабые»
│   │   ├── mistakes.ts     # Банк ошибок
│   │   ├── settings.ts     # Настройки UI в IndexedDB
│   │   ├── backup.ts       # Экспорт JSON
│   │   └── mastery.ts      # Изученность юнитов
│   ├── types.ts
│   └── styles/app.css
├── RULES.md            # Спека колод
└── vite.config.ts
```

---

## Деплой

Публичная версия для браузера (в т.ч. с телефона) — **GitHub Pages**.

GitHub Actions (`.github/workflows/deploy.yml`): при пуше в `main` — сборка и публикация на GitHub Pages с `VITE_BASE_PATH=/<repo>/`.

---

## Тесты

- **Vitest** + **happy-dom** + **fake-indexeddb**
- Порог coverage: **95%** (statements, branches, functions, lines)
- Интеграционные сценарии: `src/App.spec.ts`
- Модули: `src/lib/*.test.ts` — idb, progress, mistakes, settings, backup, mastery, patrones, loadDecks, queue
